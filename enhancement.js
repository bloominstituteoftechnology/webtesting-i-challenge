
module.exports = {
    item: (item) => {
        return {
            name: item.name,
            type: item.type,
            durability: item.durability,
            enhancement: item.enhancement
        }
    },

    success: (item) => {

    },

    fail: (item) => {

    },

    repair: (item) => {
        
    }
}