const enhancer = require('./enhancer.js');
// test away!

describe('item functions', () => {
  describe('succeed', () => {
    it('should enhance item by 1', () => {
      expect(succeed())
    })
  })
})
