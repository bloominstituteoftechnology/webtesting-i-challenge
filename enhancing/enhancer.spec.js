const enhancer = require('./enhancer.js');
// test away!

describe('enhancer.js', () => {
	describe('repair() method', () => {
		it('should return a durability of 100', () => {
			expect(enhancer.durability()).toBe();
		});
		// it('should return a durability of 100', () => {
		// 	expect(enhancer.durability()).toBe();
		// });
		// it('should return a durability of 100', () => {
		// 	expect(enhancer.durability()).toBe();
		// });
	});
});
